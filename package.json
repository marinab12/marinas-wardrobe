{
  "name": "marinas-wardrobe",
  "version": "1.0.0",
  "private": true,
  "description": "Armario digital de Marina empaquetado como app iOS.",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "build": "node scripts/prepare-web.js",
    "ios:add": "npm run build && npx cap add ios",
    "ios:sync": "npm run build && npx cap sync ios",
    "ios:open": "npm run ios:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
